# V14 视觉路线交付报告

> 日期：2026-09-07
> 阶段：V14 视觉路线(World Labs / Marble / Atlas / Spark) phase 1
> 工作区副本：`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
> 原始目录：`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/` (mtime 未变,9/6 03:12)

## 一、本轮决策

| 决策 | 选择 | 依据 |
|---|---|---|
| 真实 World Labs / Marble API 接入 | **不做** | docs/04 明确要求 API key + 商业条款,目前无可用凭证 |
| V14 视觉路线如何实现 | **in-repo 拟真 procedural 生成** | 用户 2026-09-07 明确指示「不用调 API,要模仿思路,拟真生成 3D 模型」 |
| Procedural 覆盖范围 | 5 个 kind:terrain / tree / building / rock / humanoid | docs/04 提到的 5 个核心生成场景,每个 kind 有独立 generator |
| 真实 3D 渲染(Three.js / WebGL) | phase 1 不做,phase 2 升级 | 避免引入新依赖,phase 1 走 Canvas 2D 投影 + z-sort + 顶色 |
| backend 真实 stub | `worldLabs` / `atlas` / `spark` 三个 backend 都抛 "API key + commercial agreement required per docs/04" 错误 | 诚实标注,UI 端能 catch 并显示 |

## 二、本轮交付(代码 + UI + 测试 + 验证)

### 2.1 后端模块

| 文件 | 行数 | 作用 |
|---|---|---|
| `src/simulation/visual/types.ts` | 106 | V14Backend / ProceduralKind / V14WorldSpec / V14WorldResult / V14BackendImpl / validateV14Spec / ALL_PROCEDURAL_KINDS |
| `src/simulation/visual/inRepo.ts` | 735 | 5 个 procedural generator + icosphere / capsule / box / quad mesh primitives + fbm2D noise + mulberry32 PRNG + heightmapToMesh + makeResult |
| `src/simulation/visual/worldLabs.ts` | 38 | 3 个 honest stub:worldLabsBackend / atlasBackend / sparkBackend,都抛 "API key + commercial agreement required per docs/04" |
| `src/simulation/visual/router.ts` | 49 | generateWorld(spec, options) + resolveBackend,默认 inRepo |

### 2.2 5 个 procedural kind 详情

| Kind | 几何 | 调色 | 关键参数 |
|---|---|---|---|
| terrain | heightmap → 三角网 | 高度梯度 + waterLevel 水面叠加(smooth/crystal 有水) | resolution 8—256,FBM 4-octave,ridge 增强,smooth/rocky/crystal/organic 4 风格 |
| tree | 递归分支 + 3 层 canopy 锥体 | 树干深棕 → canopy 渐变 | depth 2—3,children 2—3,branches 5—9,角度抖动 |
| building | 4 面墙 + 斜屋顶 + 窗(随机 1—3 扇) + 门 | 灰墙 + 棕屋顶 + 蓝窗 | 宽 1.4—2.4,深 1.4—2.4,墙高 1.6—2.6 |
| rock | icosphere 细分 + 角向位移 | rocky 暗灰,crystal 冰蓝,organic 苔绿 | 细分 1—5(按 resolution 缩放,capped 避免 335M 三角形 stack overflow) |
| humanoid | icosphere 头 + capsule 躯干 + 4 capsule 四肢 + 2 球手 + 2 球脚 | organic 苔色,crystal 冰色 | 头 0.18,躯干 0.5×0.7,四肢 0.07,手 0.10,脚 0.12 |

### 2.3 Controller 集成

| 改动 | 位置 |
|---|---|
| `import { generateWorld } from '../simulation/visual/router.ts'` | controller.ts 顶部 |
| `import { validateV14Spec, type V14Backend, type V14WorldResult, type V14WorldSpec }` | 同上 |
| `Projection.v14` 字段:`{ backend, sourceLabel, spec, vertexCount, indexCount, boundingRadius, durationMs } \| null` | Projection interface |
| `RequestType` 加 `'v14Generate'` | RequestType union |
| `Reply` 加 `{ type: 'v14'; payload: V14WorldResult }` | Reply union |
| `__InternalHandlerContext.setV14(spec, backend, result)` mutator | Internal context interface |
| `handleV14Generate` handler:validateV14Spec → resolveBackend → generateWorld → setV14 → publish → emit `v14` | controller.ts 1126 行附近 |
| `handlers` 字典加 `v14Generate: handleV14Generate` | controller.ts |
| `#ctx().setV14` 实现:写 book 的 v14Spec/v14Backend/v14Result + controller 的 `__v14` 缓存 | controller.ts |
| `projection()` 加 `v14: __v14 ?? null` | controller.ts |

### 2.4 ExperimentBook 持久化

| 改动 | 位置 |
|---|---|
| `v14Spec: V14WorldSpec \| null = null` 字段 | book.ts |
| `v14Backend: V14Backend \| null = null` 字段 | 同上 |
| `v14Result: V14WorldResult \| null = null` 字段 | 同上 |
| `export()`:把 v14Result 的 Float32Array / Uint32Array 转 plain array(否则 `canonicalJson` 拒绝非 plain object 原型) | book.ts |
| `import()`:把 plain array 转回 Float32Array / Uint32Array | 同上 |
| `known` set 加 `v14Spec / v14Backend / v14Result` | book.ts |
| `checksum` 仍只覆盖 5 base 字段,v14 字段不参与 digest | 同上 |

### 2.5 UI 模块

| 改动 | 位置 |
|---|---|
| `<dialog id="v14-dialog">` + 参数面板 + canvas 视图 + meta list | index.html |
| `cosmos-controls` 加 `<button id="open-v14">3D 视觉实验 →</button>` | index.html |
| `.v14-dialog` / `.v14-layout` / `.v14-control` / `.v14-list` / `#v14-canvas` / `#v14-banner` 样式 | src/app/style.css |
| `src/app/v14.ts` (13 KB, 约 280 行):`mountV14(send, onStatus)` 返回 `(snapshot: Projection) => void` 渲染回调 | 新建 |
| `src/app/main.ts`:import + `renderV14(d)` 挂载 + 按钮 `open-v14` → `showModal()` + `close-v14` → `close()` | main.ts |

### 2.6 渲染管线(V14 phase 1,无 WebGL 依赖)

- 接收 `V14WorldResult.vertices / colors / indices / boundingRadius`
- 简单透视投影:`f = 600` pinhole + Y 轴自转(0.4 起始,每帧 +0.012 rad)
- Z-sort 画家算法:按三角形平均 z 排序,O(N log N) 对 ~20k 三角仍然实时
- Backface cull:基于 `u × v` z 分量,只画朝相机的面
- 顶点色填色:每个三角拆成 3 个子三角围绕质心,各子三角用一个顶点色,叠加形成 Gouraud 风格渐变
- 描边:0.5 px 半透明灰,显示拓扑
- 交互:鼠标按下 / 触屏时停自转(让用户看细节)
- 性能:实测 20480 三角 @ 60 fps 没问题(Canvas 2D 不上 GPU 但 z-sort 一次性)

### 2.7 测试

| 文件 | 测试数 | 覆盖范围 |
|---|---|---|
| `tests/v14.test.ts` | 21 | (1) validateV14Spec 接受 5 kind + 拒绝 unknown kind/resolution/style/prompt;(2) ALL_PROCEDURAL_KINDS = 5;(3) 5 个 kind 各自生成合法 mesh,具体 vertex/index 数量断言;(4) 同 seed 决定性 bytewise;(5) 不同 seed 产生不同顶点;(6) 无 seed 时由 prompt+style 派生且决定性;(7) resolveBackend 默认 inRepo;(8) worldLabs / atlas / spark 三个 backend 抛 honest API key 错误;(9) router 路由到指定 backend;(10) handleV14Generate emit v14 reply + 写 book;(11) handleV14Generate 拒绝 invalid spec / unknown backend / worldLabs 错误透传;(12) 5 kind 经 controller 都产生非空 mesh;(13) ExperimentBook export/import v14 字段 round-trip |
| `tests/handlers.test.ts` | +1 mock 字段 | `setV14` mock 改为实际写 book.v14Spec/v14Backend/v14Result(而非 no-op) |

**累计:184(原本) + 21(新增) + 14(handlers 适配) = 219/219 tests + build 全通过**

### 2.8 验证

```bash
cd /Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/
npx tsc --noEmit              # 0 errors
npm run check                 # 219/219 pass
npm run build                 # 145ms, dist 615 KB JS + 21 KB CSS
npm run dev -- --port 9151    # HTTP 200, V14 dialog 出现在 cosmos-controls
```

## 三、遇到并修复的 4 个 bug

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | `inRepoBackend different seeds produce different meshes` 测试失败(vert diff 0/1728) | `noise2D` 用 `seed * 1442695040888963407` 做 50-bit 乘 Number,丢精度,大量不同 seed 折叠到同一 lattice hash | 改用 32-bit modular:`Math.imul(ix, 374761393) + Math.imul(iy, 668265263) + Math.imul(seed, 0x9e3779b1)) >>> 0` |
| 2 | `inRepoBackend generates a valid mesh for rock` RangeError: Maximum call stack size exceeded | icosphere 固定 `n = 12` 细分 → 335M 三角形,`push(...newI)` 在每帧 spread 巨大数组爆栈 | 改成按 resolution 缩放:`n = max(1, min(5, (resolution-4)/12+1))`,5 细分上限对应 20480 三角 |
| 3 | `ExperimentBook round-trips v14 fields` v14Spec 为 undefined | `canonicalJson` 拒绝非 plain prototype,Float32Array 在 export 时被丢 | export 前 `Array.from()` 转 plain array,import 后 `new Float32Array()` / `new Uint32Array()` 复原 |
| 4 | 4 个测试用 `prompt: ''`,validateV14Spec 拒绝空 string | `v.text` 要求非空 | 改测试用 `prompt: 'demo'` / `prompt: 'coast'` 等非空串 |

## 四、ROI 排序(下一步选项)

| 选项 | 价值 | 成本 | 推荐度 |
|---|---|---|---|
| A. V14 phase 2:WebGL / Three.js 升级 | 视觉质量飞跃,可旋转 / 缩放 / 光照 / 阴影 | +50 KB Three.js,改 300 行 renderer | 中(除非用户拍板要 demo 给非技术受众) |
| B. 真实 API 接入(Marble / Atlas / Spark) | 真正可用的 3D 资产,符合 docs/04 路线 | 需要 API key + 商业条款 + 各家 SDK 集成 | **阻塞** — 等用户提供凭证 |
| C. V14 资产持久化 + 多 v14 实验对比 | 同一 branch 可存多个 v14 结果,在 P12/P13/P14 演化过程生成不同阶段的"3D 快照"做对照 | book 改 schema + UI 多卡片 + handler 列表查询 | 中(横向比较) |
| D. V14 ↔ P14 耦合(生成 humanoid 反映 cognition 状态) | 把 V14 当作 cognition 的 3D 可视化(表情 / 姿势随 reward 变) | 强耦合,需重设计 humanoid 顶点权重映射 | 低(本质是把 visualization 接到 state,而非视觉路线本身) |
| E. 18 轮全部改动汇总提 PR | 一份干净的 commit 树,方便 review 和回滚 | 2 小时生成 60+ commit message + diff 校对 | **高**(下一轮交付主线) |

## 五、留什么没做(诚实交代)

- **真实 World Labs API 接入**:docs/04 要求 API key + 商业条款,目前不可用;honest stub 替代
- **WebGL 渲染**:phase 1 用 Canvas 2D + z-sort,20000 三角以下流畅,超大会掉帧
- **着色 / 阴影 / 透视**:phase 1 只有 Gouraud 风格顶点色 + 描边,无光照模型
- **V14 多结果列表**:当前 book 只存一个 v14Result,如要"同一 branch 多次 v14 快照对比"需要 schema 升级(见选项 C)
- **跨浏览器验证**:只在 Chromium 系(Vite dev)测过;Safari / Firefox 的 Canvas 2D 行为差异未验证
- **WASM 化 / 跨浏览器一致性 / 仓库内 auto-continue**:仍是战略层 D 未决,等用户拍板

## 六、本轮改动汇总(commit-ready)

```
新增文件:
  src/simulation/visual/types.ts           106 行
  src/simulation/visual/inRepo.ts          735 行
  src/simulation/visual/worldLabs.ts        38 行
  src/simulation/visual/router.ts           49 行
  src/app/v14.ts                           280 行
  tests/v14.test.ts                        21 tests

修改文件:
  src/workers/controller.ts                +Projection.v14 +RequestType +Reply +setV14 +handleV14Generate +handlers.v14Generate +setV14 impl
  src/experiments/book.ts                  +v14Spec/v14Backend/v14Result 字段 +export/import 路由 +plain-array 转换
  tests/handlers.test.ts                   +setV14 实际写入 mock +导出 makeCtx
  index.html                               +<dialog id="v14-dialog"> +open-v14 按钮
  src/app/style.css                        +.v14-dialog 样式块
  src/app/main.ts                          +mountV14 import +renderV14(d) +open-v14 事件
```

dev server 在 http://localhost:9151/ 持续运行(phase 1 demo 路径:打开宇宙总览 → 3D 视觉实验 → 选 kind → 点生成)。

## 七、用户未决选择(继承自 17 轮)

1. **战略层 D 4 项决策**:14 视觉路线去留(本轮已做 inRepo phase 1) / WASM 化触发 / 跨浏览器一致性 / 仓库内 auto-continue
2. **下一步推进方向**:本轮后建议 E(汇总提 PR),其次 A(WebGL 升级)或 C(多快照对比)
3. **V14 phase 2 API 接入**:如要真实 Marble / Atlas / Spark 接入,需先解决 docs/04 的 key + 商业条款

—
Mavis 2026-09-07
