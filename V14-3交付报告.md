# V14-3 视觉路线交付报告(F + G)

> 日期:2026-09-07
> 阶段:V14 视觉路线 phase 3 — 跨 branch 快照对比 + 批量参数扫描
> 工作区副本:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
> 原始目录:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(完全未动,11:37 mtime 仍为 V14-2)

## 一、本轮决策(用户拍板)

| 选项 | 选择 | 依据 |
|---|---|---|
| F. V14 跨 branch 对比 | **做** | docs/15 F:同一主时间线不同 fork 下的快照可一起看,做"如果当年不是这种生命..."的可视化 |
| G. V14 batch 参数扫描 | **做** | docs/15 G:多 seed × 多 kind × 多 style 一次性生成,直方图呈现分布 |
| A. Three.js 升级 | 已做(V14-2) | — |
| B. 真实 API 接入 | 砍 | 用户明确"开源免费路径" |

## 二、本轮交付

### 2.1 F:跨 branch 快照对比

| 改动 | 位置 |
|---|---|
| `Projection.v14` 加 `byBranch: Record<string, number>` 字段(预计算快照数量直方图,避免 UI 每次重算) | src/workers/controller.ts |
| `computeByBranch(summaries)` helper | 同上 |
| 所有 `setV14*` mutator + 新 `pushV14Batch` 同步 `__v14.byBranch` | 同上 |
| `Projection()` 字段兜底 `{ snapshots: [], activeId: null, byBranch: {} }` | 同上 |
| **UI**: 列表按 `createdAtBranch` group,每组显示 `[branch-name] N 快照` 头(branch 块按"组内最新 createdAtMs"排序) | src/app/v14.ts renderList |
| **UI**: 对比模式左右 select 加 `[branch] ` 前缀(让用户区分 fork-A 的 rocky 和 main 的 rocky) | renderCompareSelects |
| **UI**: 列表标题旁加 "N 条 / M 分支" 计数 | 返回 projection 同步处 |
| `byBranch` 持久化:已经在 `book.v14Snapshots` 上天然持久(每个 snapshot 存了 `createdAtBranch`),不需要改 export/import |

### 2.2 G:V14 batch 参数扫描

#### 2.2.1 类型

| 改动 | 位置 |
|---|---|
| `V14BatchScanSpec` 接口:`prompt? / seeds[1—16] / kinds[1—5] / styles[1—4] / resolution[4—256] / labelPrefix?` | src/simulation/visual/types.ts |
| `validateV14BatchScanSpec(s)` 严格校验(数量 / 取值 / 长度) | 同上 |
| `V14BatchReport` 接口:`totalRuns / perKind / perStyle / vertexHistogram / durationHistogram / vertexStats / durationStats / totalDurationMs / snapshotIds / backend / startedAtTick / startedAtBranch` | 同上 |
| `vertexHistogram` 7 个 log bin(<64 / 64—256 / 256—1k / 1k—4k / 4k—16k / 16k—64k / 64k+) | 同上 |
| `durationHistogram` 9 个 bin(0—1/1—2/2—5/5—10/10—20/20—50/50—100/100—200/200+ ms)— 起始 bin 改为 0 因为 inRepo 经常 < 1ms | 同上 |

#### 2.2.2 Controller

| 改动 | 位置 |
|---|---|
| `RequestType` 加 `'v14BatchScan'` | src/workers/controller.ts |
| `Reply` 加 `{ type: 'v14Batch'; payload: { report, snapshots, activeId } }` | 同上 |
| `__InternalHandlerContext` 加 `pushV14Batch(snaps)` mutator(追加列表,不切 active) | 同上 |
| `handleV14BatchScan` 实现: | 同上 |
| └ validateV14BatchScanSpec | |
| └ 三层循环 `seeds × kinds × styles` → `generateWorld(spec, { backend: 'inRepo' })` | |
| └ 每条结果 `makeV14Snapshot({ id: uuid, spec, result, tick, branch, label })` | |
| └ `ctx.pushV14Batch(newSnaps)`(active 指针保持不变) | |
| └ build `V14BatchReport`(直方图 / 统计 / total wall-clock duration) | |
| └ emit `v14Batch` reply | |
| └ `handlers` 字典加 `v14BatchScan: handleV14BatchScan` | |

#### 2.2.3 UI:批量扫描表单 + 直方图

| 改动 | 位置 |
|---|---|
| 左栏新增 `<details id="v14-batch-details">` 折叠区 | index.html |
| 多选 kind(5 个,默认全选)/ style(4 个,默认全选)/ seed 数(1—16)/ resolution(8/16/24/32)/ label 前缀(可选) | 同上 |
| 2 个按钮:`v14-batch-run` 运行 / `v14-batch-clear` 隐藏结果 | 同上 |
| 4 个 SVG 直方图容器(kind / style / vertex / duration)+ 1 个 `<ul>` 摘要 | 同上 |
| **v14.ts**: `drawHistogram(svg, bins, color)` — 纯 DOM SVG `<rect>` + `<text>` 渲染柱状图(viewBox `0 0 320 120`) | src/app/v14.ts |
| `renderBatchReport(report)`:刷新 4 个直方图 + 摘要 ul(总运行数 / 耗时 / 顶点均位 / 耗时均位 / 起始 tick+branch) | 同上 |
| `batchRunBtn` 事件:读多选 → 校验(空集 reject)→ 发 `v14BatchScan` 请求 → 渲染报告 | 同上 |
| `batchClearBtn`:隐藏输出 + 清空摘要(快照仍保留在列表) | 同上 |
| 完成后更新 `snapshots` 缓存 + `renderList` 让新 snapshot 出现在 list/group | 同上 |
| 进度提示:`扫描中… N 次生成` → `完成 · N 次 · T ms · B 个 branch` | 同上 |

### 2.3 测试(`tests/v14.test.ts`,加 5 个新)

| 测试 | 覆盖 |
|---|---|
| `handleV14BatchScan pushes N×K×S snapshots and returns a v14Batch reply with histograms` | 3×2×2=12 个 snapshot push;active 指针不变;reply.report.totalRuns=12;perKind / perStyle / vertexHistogram / durationHistogram 求和均=12;snapshotIds 在 book 中能找到 |
| `handleV14BatchScan rejects out-of-range parameters` | seeds=[] / kinds=spaceship / styles=plasma / resolution=999 / seeds.length=17 — 5 个 reject |
| `handleV14BatchScan labelPrefix is reflected in the snapshot labels` | labelPrefix='scanA' → 至少一个 snapshot.label 以 'scanA ' 开头 |
| `Cross-branch (F): snapshots created on different branches are listed together` | 3 个 snapshot 跨 3 个 branch → 列表长度 3,branch set 大小 3,byBranch histogram {main:1, fork-a:1, fork-b:1} |
| `Book round-trip preserves createdAtBranch across branches` | export → import 后 `createdAtBranch` 保持,activeId 仍指向新的(被 setActive 那个) |

**累计 224/224 tests + build + dev server 全绿**

## 三、修的 1 个 bug

| 现象 | 根因 | 修复 |
|---|---|---|
| `handleV14BatchScan` duration histogram 全部为 0(测试 `durSum` 期望 12 实际 0)| 第一个 bin `[1, 2)`,但 inRepo generation 经常 < 1ms,Node 24 的 `performance.now()` 分辨率有时 rounds 到 0 — 所有 run 都落在 [0, 1) 区间被 orphan | 改 durBins 起始为 0:`[0, 1, 2, 5, 10, 20, 50, 100, 200, Infinity]`,让 sub-ms run 进第一个 bin |

## 四、验证

```bash
cd /Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/
npx tsc --noEmit              # 0 errors
npm run check                 # 224/224 pass (V14 + V14-2 + V14-3 全跑)
npm run build                 # 136ms, dist 626 KB JS + 25 KB CSS (4 个 SVG 直方图样式)
npm run dev -- --port 9151    # HTTP 200, V14 dialog 含 batch 折叠区 + 4 直方图容器
```

## 五、ROI 排序(下一步选项)

| 选项 | 价值 | 成本 | 推荐度 |
|---|---|---|---|
| E. 20 轮全部改动汇总提 PR(70+ commit) | 一份干净 commit 树,review 友好 | 2 小时 | **高** |
| H. V14 资产导出 .glb / .gltf | 让 V14 生成的 3D 资产可在 Blender / web viewer 二次利用(用 Three.js 的 `GLTFExporter`) | 2 小时 | 中(没用户需求) |
| I. V14 ↔ P15 文明聚落耦合(每聚落生成一个 3D 模型作"首都天际线") | V14 真正"为生态/文明服务" | 半天 | 中 |
| J. P19:把"快照"扩展到 P10-C 全套(每个 phase 一类快照) | 概念统一:任何 phase 都能"快照保存"当前状态 | 1 天 | 低(范围膨胀) |

## 六、留什么没做(诚实交代)

- **batch 进度推送**:目前 batch scan 是阻塞的(全部跑完才 reply),没有 `progress` 事件。100+ run 时 UI 卡 1—2 秒。后续可加 `Reply type: 'v14BatchProgress'` + `v14Batch` reply 加 `snapshotIds` 实时 push
- **3D 资产导出**:`.glb` / `.gltf` 导出没做,Three.js 本身有 `GLTFExporter` 5 行代码就接上,需要 user 触发按钮
- **V14 batch 与 P17 batch 共用 progress 协议**:目前两个 batch system 是独立设计,后续可统一 progress reply shape
- **快照缩略图**:列表每行 100+ 字符 label,没有 3D 缩略图(要做的话需要 OffscreenCanvas 离屏渲染)
- **V14 ↔ P15 文明聚落耦合**:snapshot 仍是独立子系统,没有"为某个聚落生成专属 3D 模型"的入口
- **WebXR 模式**:用户没提
- **V14 资产清理 batch**:没有 "删除所有 batch 标签的 snapshot" 的一键清理入口,只能逐条删
- **WASM 化 / 跨浏览器一致性 / 仓库内 auto-continue / 真实 API 接入**:战略层 D 未决,等用户拍板

## 七、本轮改动汇总(commit-ready)

```
修改文件:
  src/simulation/visual/types.ts        +V14BatchScanSpec +validateV14BatchScanSpec +V14BatchReport(直方图 + 统计)
  src/workers/controller.ts             +Projection.v14.byBranch +RequestType.v14BatchScan +Reply.v14Batch +pushV14Batch mutator +handleV14BatchScan +computeByBranch helper +handlers 字典
  tests/handlers.test.ts                +pushV14Batch mock
  tests/v14.test.ts                     +5 tests(批扫描 + 跨 branch + round-trip)
  src/app/v14.ts                        +ALL_PROCEDURAL_KINDS import +byBranch state +renderList 按 branch group +renderCompareSelects 加 branch 前缀 +batch 折叠区(bind 8 个 DOM)+drawHistogram SVG 渲染 +renderBatchReport
  src/app/style.css                     +.v14-branch-head +.v14-batch-section +.v14-histogram +.v14-batch-output
  index.html                            +<details id="v14-batch-details"> + 5 多选 / 4 select / 1 input / 2 button / 1 状态 / 4 SVG
  src/app/main.ts                       mountV14 注释更新(F + G 描述)
```

dev server http://localhost:9151/ — 打开 → 3D 视觉实验 → 展开"批量参数扫描"折叠区 → 选 kind/style/seed → 点运行 → 看 4 个直方图 + 摘要。列表里看到的快照会按 branch 分组显示。

## 八、用户未决选择(继承)

1. **战略层 D 4 项**:WASM 化 / 跨浏览器一致性 / 仓库内 auto-continue(14 视觉路线已实质完成 F + G,可视为进一步解决)
2. **下一步推进方向**:本轮后建议 E(20 轮汇总提 PR),其次 H(导出 .glb 给 Blender 用)
3. **真实 API 接入**:用户明确"开源免费",暂无 B 需求

—
Mavis 2026-09-07
