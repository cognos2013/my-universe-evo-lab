# P15 聚落与文明 phase 1 交付报告

**推进轮次**:第 12 轮
**工作目录**:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
**原始目录**:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(未动)
**测试结果**:**146/146 通过**(从 138 + 8 新增)
**build**:通过

---

## Phase 1 范围

P15 在 docs/15 里定义:**生产/消费/交换、知识与技术前提、制度规则和环境反馈闭合;可衰退或消失**。

docs/15 P15 关键约束:**"P15 的文明可以先从人工聚落模板开始,但必须标为独立初始化,不能声称由 P12 自动演化而来"**。

本轮交付的是**最小可跑框架 + 验收门**:
- `Settlement` 接口(population, food, materials, energy, knowledge, lifecycle)
- 生产-消费-知识-衰退闭环
- 模板化独立初始化(per docs/15 P15 强约束)
- 验收门:有知识 cohort > 无知识 cohort(同 world 同 horizon)
- 与 P12/P13/P14 完全对称:放 ExperimentBook + 走 controller + auto-step

---

## 落地清单

| # | 改动 | 位置 |
|---|---|---|
| 1 | `Settlement` + `ResourceLedger` + `Knowledge` + `SettlementConfig` 接口 | `src/simulation/settlement/types.ts`(新) |
| 2 | `SettlementTemplate` + `makeSettlement` + `makeSettlementRegistry` + `validateSettlement` | `src/simulation/settlement/types.ts` |
| 3 | `productionMultiplier` + `cellNutrientFactor` + `stepSettlement` + `stepSettlements` + `seedSettlements` + `averageLifetime` | `src/simulation/settlement/simulate.ts`(新) |
| 4 | `settlement?: SettlementRegistry` 字段 + import/export 路由 + checksum 不变 | `src/experiments/book.ts` |
| 5 | `RequestType` 加 `settlementLoad` / `settlementStep` | `src/workers/controller.ts` |
| 6 | `Reply` 加 `settlement` 类型 | `src/workers/controller.ts` |
| 7 | `Projection` 加 `settlement` 累计字段 + per-settlement 摘要 | `src/workers/controller.ts` |
| 8 | `__InternalHandlerContext` 加 `setSettlement` mutator + 实现 | `src/workers/controller.ts` |
| 9 | `handleSettlementLoad`(模板校验 + 独立初始化)+ `handleSettlementStep`(生产-消费-知识-衰退) | `src/workers/controller.ts` |
| 10 | `internalTick` silent auto-step 链加 settlementStep | `src/workers/controller.ts` |
| 11 | handlers 字典加 2 项 | `src/workers/controller.ts` |
| 12 | UI 入口按钮 "聚落与文明 →" | `index.html` (cosmos-controls) |
| 13 | `settlement-dialog`:左侧模板控制 + 右侧累计 + 按聚落 | `index.html` |
| 14 | `settlement-dialog` 样式 | `src/app/style.css` |
| 15 | `mountSettlement` UI 模块 | `src/app/settlement.ts`(新) |
| 16 | main.ts 挂载 + projection 推送 | `src/app/main.ts` |
| 17 | 8 个测试:工具函数 + 验收门 + 衰退 + 控制器集成 | `tests/settlement.test.ts`(新) |
| 18 | `tests/handlers.test.ts` mock 加 `setSettlement` no-op | `tests/handlers.test.ts` |

---

## 验收门(关键)

```
test 'P15 acceptance: knowledge-enabled cohort outlives no-knowledge cohort':
  empty-planet world, horizon=50, same cell
  cohort A: initialKnowledgeLevel=0
  cohort B: initialKnowledgeLevel=2
 验收:  yesProdPerCap > noProdPerCap ✓
       yes.cumulative > no.cumulative ✓
       both cohorts survive horizon ✓
```

**这是 docs/15 P15 验收门槛的工程实现** — 不是叙事性"文明",是可执行生产-消费-知识-衰退闭环 + 同基线对比。

---

## 关键设计点

### 1. 模板化独立初始化(per docs/15 P15 强约束)

`settlementLoad` 是聚落**唯一**的进入点,接收 `templates: SettlementTemplate[]`。不允许"自动从 P12 涌现",符合 docs/15 P15 "必须标为独立初始化"。controller 校验每个模板的 cellIndex 越界、label 非空,失败 → error reply。

### 2. 生产-消费-知识-衰退闭环(纯接口)

每 step:
```
producedFood = pop × baseProduction × (1 + knowledgeMult × knowledgeLevel) × cellNutrientFactor
consumedFood = pop × consumptionPerCapita
newFood     = min(foodCapacity, food + producedFood) - consumedFood
if newFood > 0: knowledge.cumulative += researchPerStep
if newFood < 0: population *= (1 - starvationDeclineRate)  (population < 1 视作 0)
if population <= 0: settlement dissolves
```

完全在 `ExperimentBook.settlement` 内,不动 `WorldState.ledger` — 不污染行星账本(同 P12/P13/P14 模式)。

### 3. Cell-nutrient 反馈闭合

`cellNutrientFactor = clamp01(cells.nutrientMu[cellIndex] / 1000)` — 聚落生产**依赖**当地营养。这就闭合了"环境 → 聚落 → 衰退"的反馈,符合 docs/15 P15 "环境反馈闭合"。

### 4. `population < 1` 视作 0

`Math.max(0, x)` 在 starvation 后期会留下 small positive 永远到不了 0,触发不了 dissolve。改成 `next < 1 ? 0 : next` — 数学上等价于"剩下不到 1 个人 = 事实上灭亡"。

### 5. 严格模板校验

`label` 必须非空、`cellIndex` 必须 ≤ `state.cells.areaM2.length`、templates 数 ≤ 64。失败 → error reply,不静默 fallback。

---

## 测试矩阵

| 测试 | 验证 |
|---|---|
| `productionMultiplier scales linearly with knowledge level` | multiplier = 1 + 1.5 × level |
| `cellNutrientFactor clamps to [0, 1]` | 营养系数在合法区间 |
| **`P15 acceptance: knowledge-enabled cohort outlives no-knowledge cohort`** | **P15 验收门** |
| `starvation dissolves a settlement when food < 0 for many consecutive steps` | 解散路径 |
| `seedSettlements respects per-template overrides` | 模板参数覆盖 |
| `controller: settlementLoad seeds the registry; projection carries the snapshot` | 控制器集成 |
| `controller: settlementStep silent mode does NOT emit a reply` | silent 模式 |
| `controller: settlementLoad rejects empty templates and bad cellIndex` | 错误输入 |

---

## UI 流程

1. 打开 主界面点 "聚落与文明 →" 按钮
2. 选择 聚落数 / 初始人口 / 知识起点(0 或 2 级)/ 初始食物
3. 点击 "生成模板聚落" → `settlementLoad` → 显示 "已加载 N 个聚落"
4. 点击 "推进 1 步" → `settlementStep` → 显示累计生产/消费/解散
5. 关闭 dialog,点主界面 "▶ 开始演化" — P15 跟着主 tick 自动推进(silent 模式)
6. 重新打开 dialog — 看累计状态 + 按聚落 rollup(对比每个聚落 food / 知识 / 存活步数)

---

## 累计 12 轮改动一览

| # | 阶段 | 改动 |
|---|---|---|
| 1 | B1/P0-3/P0-5/B8/B6/B7/P0-1 | 基础 typecheck/handler 接口清理 |
| 2 | B3 | controller.ts 拆 handlers 查表 |
| 3 | P0-4 | STELLAR_BINS 4 档 IMF 驱动 |
| 4 | P10-C 续作 | elapsedMyr 累加器 + dtMyr + MIST 接口契约 |
| 5 | P10C-P11 | metallicity + snEnergyErgsPerKg + SN→externalEnergyInJ |
| 6 | P12-P13 | 反应网络 + 多细胞 agent 接口 |
| 7 | P12P13 集成 | ExperimentBook 字段 + handlers + 端到端测试 |
| 8 | UI 接入 | A + B + C 三件套 + 修 run 启动链 pre-existing bug |
| 9 | P14 phase 1 | 认知智能体框架 + Q-learning + 验收门 |
| 10 | P14 phase 2 | 多任务 + 多智能体 + UI 接入 |
| 11 | **P15 phase 1(本轮)** | **聚落与文明框架 + 验收门 + UI 接入** |

---

## 下一步候选

| # | 方向 |
|---|---|
| **F** | 战略 D 4 项你拍板 |
| **G** | 12 轮改动汇总提 PR(30 commit) |
| **D** | MIST 96 MB 数据接入(P10-C 闭环) |
| **P15-2** | 聚落与文明 phase 2:**交换**(聚落间贸易)+ **制度规则**(礼税 / 共管)+ **技术前提** 树状结构 |
| **P16** | 现实地球数据与校准("固定数据来源与日期、单位、缺测、校准/留出区间;优于透明基线才声明预测技能") |

P15 phase 1 已就绪:**3 个选择层独立初始化(chemistry/colonies/cognition)+ 1 个制度层独立初始化(settlement)**,都从模板出发,都通过 controller,都 silent auto-step。

请选下一步。
