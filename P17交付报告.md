# P17 未来情景集合 phase 1 交付报告

**推进轮次**:第 15 轮
**工作目录**:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
**原始目录**:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(未动)
**测试结果**:**162/162 通过**(从 154 + 8 新增)
**build**:通过
**dev server**:**http://localhost:9151/**(后台运行,被前一个 vite 占用 9151 → 继续服务)

---

## Phase 1 范围

docs/15 P17:**"有版本的驱动假设、多种子/参数组合、失败统计和结果分布;可与留出历史对照评估"**

本轮交付:
- `Scenario` + `BatchConfig` + `BatchOutcome` + `BatchReport` 接口
- `runBatch` 串行跑 N 个 seed,每个 seed 一个 `SimulationController` 实例跑 K 步,记录 population / 温度 / 谱系数
- `summarise` 统计每 metric 的 7 个数字(n/mean/std/min/p5/p50/p95/max)
- 失败 seed 计入 `failures`,崩溃 seed 计入 `crashes`,不污染 summary
- 验收门:多 seed 产出**非平凡分布**(std > 0 或 p95−p5 > 0)
- `batchRun` controller handler + Projection 携带 `batches[]`
- UI 接入 `batch-dialog`

---

## 落地清单

| # | 改动 | 位置 |
|---|---|---|
| 1 | `Scenario` / `BatchConfig` / `BatchOutcome` / `BatchReport` / `StatSummary` 接口 + `validateScenario` | `src/simulation/batch/types.ts`(新) |
| 2 | `summarise` (mean/std/quantile) + `runBatch` (N seed 串行) + `runOneSeed` (新建 controller 跑 K 步) | `src/simulation/batch/run.ts`(新) |
| 3 | `ExperimentBook.batches: BatchReport[]` 字段 + import/export 路由 + checksum 不变 | `src/experiments/book.ts` |
| 4 | `RequestType` 加 `batchRun` / `Reply` 加 `batch` / `Projection.batches` / `__InternalHandlerContext.pushBatch` mutator | `src/workers/controller.ts` |
| 5 | `handleBatchRun`(同步跑批,append to book,emit reply) | `src/workers/controller.ts` |
| 6 | handlers 字典加 1 项 + mock 加 `pushBatch` no-op | `src/workers/controller.ts` + `tests/handlers.test.ts` |
| 7 | UI 入口按钮 "多种子批量 →" | `index.html` (cosmos-controls) |
| 8 | `batch-dialog`:左侧控制(scenario/resolution/ticks/seeds/seedStart/run) + 右侧累计 + 3 段 summary + per-seed | `index.html` |
| 9 | `.batch-dialog` / `.batch-layout` 样式 | `src/app/style.css` |
| 10 | `mountBatch` UI 模块 + main.ts 挂载 + projection 推送 | `src/app/batch.ts`(新)+ `src/app/main.ts` |
| 11 | 8 个新测试:summary 正确性 / 空输入 / 2 seed / 4 seed 验收门 / seedCount 上限 / 未知 version / 控制器集成 / 错误处理 | `tests/p17.test.ts`(新) |

---

## 验收门(关键)

```
test 'runBatch: outcomes across many seeds at 50 ticks have non-zero variance (acceptance gate)':
  scenario: two-lineages @ 320, 4 seeds, 50 ticks
  验收: summary.population.std > 0 || (p95 - p5) > 0 ✓
  failures = 0 ✓
```

**这是 docs/15 P17 验收门槛的工程实现** — 不是单次结果,是**结果分布**。多个 seed 跑同一 scenario,统计 population / 温度 / 谱系数分布。失败 seed 计入但不污染 summary,符合 P17 "失败统计"要求。

---

## 关键设计点

### 1. 串行执行 + 每 seed 独立 controller

每个 seed 用**新建的** `SimulationController`(不是同一个),保证状态隔离。串行执行是 phase 1 妥协:没 worker pool,跑批会阻塞 UI(几十秒)。Phase 2 可以用 worker pool 并行化。

### 2. Polling tick 等待完成

`runOneSeed` 调 `controller.handle({ type: 'run', ticks: K })` 后,**轮询** `snapshotState().tick` 等到 ≥ K(60s 上限)。这是因为 `run` 触发 setTimeout(0) 链,handler 不等内部 tick 完成。

### 3. `Math.exp(-Math.pow(x, 2))` 而非 `**`

延续 P16 教训:JavaScript `**` 优先级陷阱。`exp(-x**2)` 解析为 `exp(-(x**2))`,实际是 `exp(-x²)`,看似 OK。但 `exp(-x)**2` 解析为 `(exp(-x))²`,结果平方后丢符号!统一用 `Math.pow` 消除歧义。

### 4. Summary 函数独立可测

`summarise(outcomes)` 接收 `BatchOutcome[]`,纯函数,容易 unit test。我用 100 个 synthetic outcomes 测了 mean = 50.5、p5 = 5.95、p50 = 50.5、std ≈ 28.86 — 全部匹配手算。

### 5. 失败与崩溃分别计数

`failures`:engine error(seed 完全没跑起来)
`crashes`:population ≤ 0(世界死了)

summary 排除 failed outcomes(`filter(Number.isFinite)`),但保留 crashed 计入统计。Phase 2 可以加 `crashedSeparateStats: StatSummary` 单独统计。

### 6. 报告持久化

`ExperimentBook.batches: BatchReport[]` — 多个 report 累积。`Projection.batches` 暴露给 UI,最近一个 report 详情渲染。

---

## 测试矩阵(8 个新测试)

| 测试 | 验证 |
|---|---|
| `summarise: produces consistent mean / std / quantiles` | 100 个 synthetic outcomes → mean=50.5 / std≈28.86 / p5=5.95 / p50=50.5 |
| `summarise: empty input` | n=0,所有字段=0 |
| `runBatch: two seeds at 10 ticks` | seedCount=2 → 2 outcomes + summary |
| **`runBatch: outcomes across many seeds at 50 ticks have non-zero variance`** | **P17 验收门** |
| `runBatch: rejects seedCount > 64` | input validation |
| `runBatch: rejects unknown scenario version` | version check |
| `controller: batchRun produces a batch reply + adds to Projection.batches` | 控制器集成 |
| `controller: batchRun rejects unknown scenario` | error reply |

---

## dev server 状态

| URL | 用途 |
|---|---|
| **http://localhost:9151/** | 本机 |
| **http://192.168.3.43:9151/** | 局域网 |

后台 vite 仍在跑(被前一个任务占用端口),HTTP 200,5 个 dialog 全部加载(cosmos/galaxy/prebiotic/cognition/settlement/earth/batch)。

---

## 测试路径

1. http://localhost:9151/
2. 点 "多种子批量 →"
3. 选 scenario `two-lineages` / res `320` / ticks `50` / seeds `4`
4. 点 "运行批量"(等几秒,看 status)
5. 右侧显示:scenario / seed 范围 / 完成 4/0/0
6. 3 段 summary:population / temperature / lineages 各自 mean / std / p5 / p50 / p95
7. Per-seed 列表(4 条):每个 seed 的 pop / T / lin
8. 切到 `empty-planet` 再跑,看 pop 始终 0(empty-planet 不播种)

---

## 累计 15 轮一览

| 阶段 | 轮次 | 状态 |
|---|---|---|
| 基础 | 1-7 | ✅ |
| 集成 | 8 | ✅ |
| P14 (智能行为) | 9-10 | ✅ |
| P15 (聚落与文明) | 11-13 | ✅ |
| P16 (地球校准) | 14 | ✅ |
| **P17 (多种子批量)** | **15** | **✅ phase 1** |

**docs/15 P12 / P13 / P14 / P15 / P16 / P17 全部 phase 1 落地**。

---

## 下一步候选

| # | 方向 |
|---|---|
| **F** | 战略 D 4 项你拍板 |
| **G** | 15 轮改动汇总提 PR(45 commit) |
| **D** | MIST 96 MB 数据接入(P10-C 闭环) |
| **V** | 14 视觉路线(World Labs / Marble) |
| **P17-2** | 多种子并行化(worker pool) + 参数扫描(非仅 seed) + 与 holdout 历史对比 |
| **P10-2** | 真实 GARD / Markov-state 化学数据接入 |

P12-P17 phase 1 全部就绪。**15 轮总改动** 等汇总提 PR / 选 D 收 P10-C / 选 V 重启 14 视觉路线。

请选下一步。
