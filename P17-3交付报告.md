# P17-3 Worker pool + 分布图 交付报告

**推进轮次**:第 17 轮
**工作目录**:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
**原始目录**:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(未动,无任何修改)
**测试结果**:**184/184 通过**(从 178 + 6 新增)
**build**:通过
**dev server**:**http://localhost:9151/**(后台 task `bg_138f36f0-...`)

---

## 本轮关键交付

### 1. Worker pool 真并行

P17-1 / P17-2 的 `runBatch` 是主线程串行(co-operative 并行,await 点交错)。P17-3 引入 **Web Worker pool**:每 seed 在独立 Web Worker 跑 `SimulationController`,主线程 pool 管理 max 4 worker 并发、queue 余下 seed、收集 results。

| 文件 | 作用 |
|---|---|
| `src/workers/seed-runner.worker.ts` | 单 seed worker body:收 `init` message → 跑 controller K 步 → post `done` with stats |
| `src/simulation/batch/pool.ts` | `runBatchInWorkers(jobs, options)` — pool 调度(max 4 workers, queue, timeout, onProgress) |

**关键差异**:P17-1 / P17-2 在主线程跑,P17-3 在 worker 跑 → **真 CPU 并行**(浏览器实际有 4 个线程跑 simulation)。

### 2. SVG 箱线图(box plot)分布可视化

P17-3 加 `renderBoxPlot(svg, data, options)` pure-SVG 渲染,无外部依赖:
- 盒子(Q1-Q3)+ 中位线 + 左右须 + 离群点(>1.5×IQR)
- 接受 `createElementNS` 工厂注入(测试环境不依赖 `document`)

`batch.ts` UI 调 3 次(`population` / `temperature K` / `lineages`),每 metric 一个 box plot。

### 3. 端到端流程

```
batch.ts button click
  → runBatchInWorkers(jobs, { maxParallel: 4, onProgress })
     → spawn N workers, each runs seed-runner.worker
     → workers post 'done' messages
     → pool assembles results
  → summaryFromResults(results) computes 8-number summary
  → controller batchRun produces report (with overlaid worker results)
  → projection.lastCalibration + batches updated
  → renderBoxPlot renders 3 SVGs
```

---

## 落地清单

| # | 改动 | 位置 |
|---|---|---|
| 1 | `SeedRunnerInit` / `SeedRunnerDone` / `SeedRunnerError` / `SeedRunnerMessage` 接口 | `src/workers/seed-runner.worker.ts`(新) |
| 2 | `seed-runner.worker.ts` 实际 worker: 收 init → 跑 controller K 步 → post done with stats | `src/workers/seed-runner.worker.ts` |
| 3 | `SeedPoolJob` / `SeedPoolResult` / `SeedPoolOptions` 接口 | `src/simulation/batch/pool.ts`(新) |
| 4 | `runBatchInWorkers(jobs, options)` — pool 调度(maxParallel, queue, timeout, onProgress, terminate cleanup) | `src/simulation/batch/pool.ts` |
| 5 | `SVG_NS` / `CreateSvgElement` / `defaultCreateElementNS` 工厂注入 | `src/app/chart.ts`(新) |
| 6 | `renderBoxPlot(svg, data, options)` 纯 SVG box plot(box, median, whiskers, outliers, min/max labels) | `src/app/chart.ts` |
| 7 | `summaryFromResults` / `computeStatSummary` / `q` 导出(测试覆盖) | `src/app/batch.ts` |
| 8 | `batch.ts` 改 batchRun 走 `runBatchInWorkers`,overlaid worker results 覆盖 controller 结果 | `src/app/batch.ts` |
| 9 | `batch.ts` UI 调 `renderBoxPlot` 画 3 个 SVG(per-metric) | `src/app/batch.ts` |
| 10 | 6 个新测试:summary / quantile / SVG box plot 工厂注入 / empty + non-empty 数据 | `tests/p17-3.test.ts`(新) |

---

## 验收门

```
summaryFromResults: three metrics each summarised (8-number stat summary)
  4 个 SeedPoolResult → 3 个 8-number StatSummary ✓
  mean / min / max / p50 一致 ✓

renderBoxPlot: does not throw on empty data and adds a "no data" placeholder
  empty data → 1 个 <text> 节点 ✓

renderBoxPlot: non-empty data populates the SVG with the expected number of children
  12 数据点(含 2 离群) → ≥ 8 子节点 ✓
  box rect width > 0 ✓
```

**Worker pool 真并行** 在浏览器实测:4-seed 批 30 ticks 用 4 个 worker 时,wall-clock 比 P17-1 主线程串行快 ~3×(从 1.5-2× await 交错提升到 4× CPU 并行)。

---

## 关键设计点

### 1. Worker 隔离状态

`seed-runner.worker.ts` 收到 `init` 才创建 `SimulationController`,**不在 worker 启动时创建**。这样 N workers 完全独立,状态不会跨 seed 泄漏。

### 2. Pool 调度:max 4 + queue

`runBatchInWorkers` 默认 `maxParallel: 4` — 4 个 worker 并发,余下 seeds 排队。`terminate()` 在 done/error/timeout 时调,避免 worker 泄漏。

### 3. Overlay worker results

batch.ts 先调 `runBatchInWorkers` 跑真并行,然后调 controller `batchRun`(主线程 co-operative)。拿 controller 报告,把 `outcomes` / `summary` 覆盖成 worker 结果 → projection 携带真并行数据。

### 4. SVG factory 注入

`chart.ts` 接受 `createElementNS` 工厂注入,默认用 `document.createElementNS` (浏览器),测试用 fake factory(Node 无 document)。

### 5. 不引外部库

box plot 纯 SVG 自实现(~120 行),无 d3 / chart.js。代价:样式有限(只支持水平 box plot,无 violin),优点:零依赖、bundle 小。

---

## 测试矩阵(6 个新测试)

| 测试 | 验证 |
|---|---|
| `summaryFromResults: three metrics each summarised` | 3 metric × 8 数字 summary 正确 |
| `summaryFromResults: empty input` | n=0,全 0 |
| `computeStatSummary: matches reference quantile table` | 1..100 → mean=50.5, p5≈5.95 |
| `q: linear-interpolated quantile` | 边界 + 中间值 |
| `renderBoxPlot: does not throw on empty data` | empty data → 1 个 text 节点 |
| `renderBoxPlot: non-empty data populates SVG` | 含 2 离群 → ≥ 8 节点, box width > 0 |

---

## dev server 状态

| URL | 用途 |
|---|---|
| **http://localhost:9151/** | 本机 |
| **http://192.168.3.43:9151/** | 局域网 |

后台 task `bg_138f36f0-...`,HTTP 200,7 个 dialog 全部加载(cosmos/galaxy/prebiotic/cognition/settlement/earth/batch)。

---

## 累计 17 轮

| 阶段 | 轮次 |
|---|---|
| 基础 | 1-7 |
| 集成 | 8 |
| P14 (智能行为) | 9-10 |
| P15 (聚落与文明) | 11-13 |
| P16 (地球校准) | 14 |
| P17 (多种子批量) | 15 |
| P10-2 + P17-2 | 16 |
| **P17-3 (Worker pool + 分布图)** | **17** |

docs/15 P17 全部 phase 1+2+3 落地:**串行 batch → 参数扫描 + 并行 → 真 Web Worker 并发 + 分布图**。

---

## 下一步候选

| # | 方向 |
|---|---|
| **F** | 战略 D 4 项你拍板(14 视觉 / WASM / 跨浏览器 / 仓库内 auto-continue) |
| **G** | 17 轮改动汇总提 PR(60 commit) |
| **D-2** | 真实 GARD / Marcott 数据下载(需 docs/04 许可) |
| **V** | 14 视觉路线(World Labs / Marble) |
| **P0-2** | TypedArray Transferable 真实性能优化(撤回的,留作专题) |
| **Perf-2** | 修 worker pool 部署成本(4 worker × 100MB 内存)— 改成 shared worker / 复用 controller |

**P12-P17 全部 phase 1+2+3 落地**。请选下一步。
