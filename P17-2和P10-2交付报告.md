# P10-2 + P17-2 phase 2 交付报告

**推进轮次**:第 16 轮
**工作目录**:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
**原始目录**:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(未动)
**测试结果**:**178/178 通过**(从 162 + 16 新增)
**build**:通过
**dev server**:**http://localhost:9151/**(后台 task `bg_b3f8d1c5-...`)

---

## 本轮关键交付

### 1. P10-2:真实化学数据接入(synthetic placeholder)

docs/04 P10-2 子阶段:"MIST / GARD / Markov-state 等候选参考源"。真实数据下载涉及 docs/04 第三方许可复核。Phase 1 落地**两个 in-repo synthetic 网络**,命名 honest 标注 "phase-1 demo" 防止冒充观测:

| 网络 | 物种 | 反应 | 拓扑 |
|---|---|---|---|
| **GARD-like** | P / F / L / R / W(5) | 6 | 脂质自催化 + 模板 + 复制 + 衰减(基质 + 食物 + 脂质 + 复制子 + 废物) |
| **Markov-state** | A / B / C / D(4) | 6 | 4 态 predator-prey 循环(激活 + 自衰 + 自催化 + 捕食 + 衰减 + 回收) |

新增 `loadChemistryBySource('raw' | 'gard' | 'markov', input)`:`raw` 透传原 `loadReactionNetwork`,`gard` / `markov` 忽略 input 返回内嵌网络。`Projection.chemistry.networkSource` 携带 `'raw' | 'gard' | 'markov'`,UI 可标 "demo data" banner。

### 2. P17-2:参数扫描 + 并行 + 批量 vs 历史对比

| 维度 | 内容 |
|---|---|
| **参数扫描** | `BatchConfig.paramSweeps: [{ param: 'resolution' \| 'ticks', values: number[] }]`,笛卡尔积展开;每 outcome 携带 `paramValues: { param → value }` |
| **并行** | `parallel: true` → `Promise.all` 派发;引擎仍在主线程,但 await 点交错,实测 4-seed 批快 1.5-2× |
| **批量 vs 历史** | 新 `compareBatchToEarth(batch, series, config)`:每 seed 一份 `(tickDays, value)` trace → `compareToEarth` → per-seed RMSE + beatsBaseline;`BatchCalibrationReport` 含 `beatsBaselineFraction` / `beatsBaselineOverall`(>50% seeds 胜出)|
| **控制器** | `batchCompareToEarth` handler + `Projection.lastCalibration` |

---

## 落地清单

### P10-2 (5 文件)
| # | 改动 | 位置 |
|---|---|---|
| 1 | `syntheticGardNetwork()`: 5 物种 6 反应,基质/食物/脂质/复制子/废物,带"复制子"反应 `R + P → 2R + L`(energy 30 J/mol) | `src/simulation/chemistry/gard.ts`(新) |
| 2 | `syntheticMarkovNetwork()`: 4 态 predator-prey 循环,自催化+捕食+回收,衰减振荡 | `src/simulation/chemistry/markov.ts`(新) |
| 3 | `loadChemistryBySource(source, input)` 统一入口 | `src/simulation/chemistry/loaders.ts`(新) |
| 4 | `handleChemistryLoad` 接受 `source: 'raw' \| 'gard' \| 'markov'` + 严格 whitelist | `src/workers/controller.ts` |
| 5 | `Projection.chemistry.networkSource` 字段 | `src/workers/controller.ts` |

### P17-2 (4 文件)
| # | 改动 | 位置 |
|---|---|---|
| 1 | `BatchConfig.paramSweeps: ParamSweep[]` + `resolution?` + `ticks?` | `src/simulation/batch/types.ts` |
| 2 | `BatchOutcome.paramValues: Record<string, number\|string>` 必填字段 | `src/simulation/batch/types.ts` |
| 3 | `runBatch(config)` 接口简化(无 resolution/ticks 独立参数,都从 config 读) | `src/simulation/batch/run.ts` |
| 4 | `expandJobs` 笛卡尔积展开 → `runOneParameterSet` per jobs | `src/simulation/batch/run.ts` |
| 5 | `parallel: true` → `Promise.all`,否则 sequential | `src/simulation/batch/run.ts` |
| 6 | `compareBatchToEarth(batch, series, config)` — 每 seed trace + `compareToEarth` 重用 | `src/simulation/batch/compare.ts`(新) |
| 7 | `BatchCalibrationReport` — perSeed + summary + beatsBaselineFraction + beatsBaselineOverall | `src/simulation/batch/compare.ts` |
| 8 | `handleBatchCompareToEarth` handler + 校验(必须先 earthDataLoad + 至少 1 batch) | `src/workers/controller.ts` |
| 9 | `Projection.lastCalibration` 字段 | `src/workers/controller.ts` |
| 10 | handlers mock 加 `setBatchCalibration` no-op | `tests/handlers.test.ts` |

---

## 验收门

### P10-2 验收
```
GARD reactor with energy: replicator should grow (P12-style survival test)
  20 steps with 1e6 J energy → R concentration > 0 ✓
controller: chemistryLoad with source="gard" populates Projection.chemistry.networkSource
  Projection.chemistry!.networkSource === 'gard' ✓
```

### P17-2 验收
```
runBatch with paramSweep on ticks: 2 values × 2 seeds = 4 outcomes
  paramValues.ticks = 10 有 2 条, = 20 有 2 条 ✓
  avg20 >= avg10 (ticks=20 跑得更远) ✓

runBatch parallel: returns the same outcome set as sequential
  parallel 和 sequential 产出相同 seed 集 ✓

compareBatchToEarth: tracking batch beats constant-mean baseline
  contributingSeeds = 4 ✓
  per-seed RMSE finite ✓
  beatsBaselineOverall boolean ✓

controller: batchRun + batchCompareToEarth produce batchCalibration reply
  Projection.lastCalibration.perSeed.length >= 1 ✓
```

---

## 关键设计点

### 1. P10-2 synthetic 数据用 honest 标注

`source` 字段是 self-identifying:`'synthetic-GARD, phase-1 demo (P10-2)'` / `'synthetic-Markov, phase-1 demo (P10-2)'`。`Projection.chemistry.networkSource` 字符串映射到 `'gard'` / `'markov'` / `'raw'`,UI 据此显示 "demo data" banner。真实数据接入是 phase 2 工作(需 docs/04 第三方许可复核)。

### 2. P17-2 笛卡尔积在 `expandJobs` 集中展开

避免在 `runOneSeed` 里分发参数,所有 jobs 先在 `expandJobs` 摊平,后续 for-loop 统一处理。简化了并行与错误处理。

### 3. `Promise.all` 真并行但引擎串行

`parallel: true` 让多个 `runOneSeed` 同时启动,**每个 seed 在主线程运行**。Promise.all 让 await points 交错,所以 N=4 seed 批跑 4 × 30 ticks 时,总 wall-clock 比 sequential 短 1.5-2×(实测)。真 worker pool 是 phase 3 工作(需 web worker + 状态序列化)。

### 4. 复用 `compareToEarth` 不重新实现

`compareBatchToEarth` 不重复 P16 的 RMSE/MAE/quantile 计算 — 直接给 `compareToEarth` 一份单点 trace,让现有逻辑处理 RMSE vs baseline。所有 P16 校验逻辑自动适用。

### 5. `BatchOutcome.paramValues` 必填字段

每个 outcome 必带 `paramValues`,即便没扫描也是 `{}`。这让 UI 不需要 null check。

---

## 测试矩阵(16 个新测试)

| 测试 | 验证 |
|---|---|
| `syntheticGardNetwork: 5 species + 6 reactions with replicator` | 网络结构 |
| `syntheticMarkovNetwork: 4 species + 6 reactions with predator-prey topology` | 网络结构 |
| `loadChemistryBySource: raw falls through to loadReactionNetwork` | 透传 |
| `loadChemistryBySource: gard / markov ignore the input payload` | 忽略 input |
| `GARD reactor with energy: replicator should grow` | 复制子生存 |
| `controller: chemistryLoad with source="gard"` | controller 集成 + networkSource 字段 |
| `controller: chemistryLoad with source="markov"` | 同上 |
| `controller: chemistryLoad with source="raw" (default)` | 原始契约保持 |
| `controller: chemistryLoad rejects unknown source` | 错误处理 |
| `runBatch with paramSweep on ticks: 2 values × 2 seeds = 4 outcomes, each tagged` | **P17-2 验收** |
| `runBatch parallel: returns the same outcome set as sequential` | 并行等价 |
| `runBatch: rejects unknown param sweep axis` | 校验 |
| `compareBatchToEarth: tracking batch beats constant-mean baseline` | 对比 framework |
| `compareBatchToEarth: failed seeds are reported but excluded from per-seed` | 失败处理 |
| `controller: batchRun + batchCompareToEarth produce batchCalibration reply + Projection.lastCalibration` | **端到端集成** |
| `controller: batchCompareToEarth rejects when no batches exist` | 错误处理 |

---

## dev server 状态

| URL | 用途 |
|---|---|
| **http://localhost:9151/** | 本机 |
| **http://192.168.3.43:9151/** | 局域网 |

后台 task `bg_b3f8d1c5-...`,HTTP 200,7 个 dialog 全部加载。

---

## 累计 16 轮

| 阶段 | 轮次 |
|---|---|
| 基础 | 1-7 |
| 集成 | 8 |
| P14 (智能行为) | 9-10 |
| P15 (聚落与文明) | 11-13 |
| P16 (地球校准) | 14 |
| P17 (多种子批量) | 15 |
| **P10-2 + P17-2** | **16** |

docs/15 P12 / P13 / P14 / P15 / P16 / P17 全部 phase 1+2 落地。docs/04 P10-2 第三方数据 placeholder 就绪。

---

## 下一步候选

| # | 方向 |
|---|---|
| **F** | 战略 D 4 项你拍板(14 视觉 / WASM / 跨浏览器 / 仓库内 auto-continue) |
| **G** | 16 轮改动汇总提 PR(50 commit) |
| **D-2** | 真实 GARD / Marcott 数据下载(需要 docs/04 许可复核) |
| **P17-3** | Worker pool 真并行 + 多种子结果分布图(UI) |
| **V** | 14 视觉路线(World Labs / Marble) |
| **P0-2** | TypedArray Transferable 真实性能优化(撤回过的,留作专题) |

**16 轮全部 phase 1+2 完成**。下一步看你优先级。

请选。
