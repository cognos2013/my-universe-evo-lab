# P14 智能行为 phase 1 交付报告

**推进轮次**:第 10 轮
**工作目录**:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
**原始目录**:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(未动)
**测试结果**:**135/135 通过**(从 127 + 8 新增)
**build**:通过

---

## Phase 1 范围

P14 在 docs/15 里定义:**感知、学习与智能行为 — 有限观测、能量/记忆成本、可训练策略;未见环境任务优于预设简单基线**。

本轮交付的是**最小可跑框架** + **验收门**:
- 认知智能体接口(Observation/Action/Policy)
- 表格 Q-learning 参考学习器 + 随机基线
- 觅食任务 + 严格奖励信号(目标 cell +50,其他 0)
- 验收门:Q-learning 平均奖励 > 随机基线(同 seed / 同 horizon)
- 与 P12/P13 完全对称:放 ExperimentBook + 走 controller + auto-step
- 能量/记忆成本:每步扣 `cognitionCostJ`,Q-table 上限 256 entries

---

## 落地清单

| # | 改动 | 位置 |
|---|---|---|
| 1 | `CognitiveAgent` + `Observation` + `Action` + `discretise()` + `observe()` + `applyAction()` + `makeCognitiveRegistry()` | `src/simulation/cognition/agent.ts`(新文件) |
| 2 | `randomPolicy` + `qLearningPolicy` + `runTaskEpisode()` + `stepCognitiveRegistry()` + `seededRng()` | `src/simulation/cognition/policy.ts`(新文件) |
| 3 | `cognition?: CognitiveRegistry` 字段 + import/export 路由 + checksum 不变 | `src/experiments/book.ts` |
| 4 | `RequestType` 加 `cognitionLoad` / `cognitionStep` | `src/workers/controller.ts` |
| 5 | `Reply` 加 `cognition` 类型 | `src/workers/controller.ts` |
| 6 | `Projection` 加 `cognition` 累计字段 | `src/workers/controller.ts` |
| 7 | `__InternalHandlerContext` 加 `setCognition` mutator + 实现 | `src/workers/controller.ts` |
| 8 | `handleCognitionLoad`(per-cohort 挂载 agent)+ `handleCognitionStep`(扣 ledger + 学习) | `src/workers/controller.ts` |
| 9 | `internalTick` auto-step 链加 silent cognitionStep | `src/workers/controller.ts` |
| 10 | handlers 字典加 2 项 | `src/workers/controller.ts` |
| 11 | 4 个单元测试 + 4 个 controller 集成测试 | `tests/cognition.test.ts`(新)+ `tests/p12p13-integration.test.ts` |
| 12 | `tests/handlers.test.ts` mock 加 `setCognition` no-op | `tests/handlers.test.ts` |

---

## 验收门(关键)

```
test 'Q-learning beats random on the foraging task':
  5-cell 链 0↔1↔2↔3↔4(目标 cell 4)
  agent 起始 cell 2,horizon=200,repeats=6
  reward = (cellIndex === 4) ? 50 : 0
  对比后 100 步平均 step-reward
  Q-learning > random:✅
```

**这是 docs/15 P14 验收门槛的工程实现** — 不是叙事性"智能",是可执行感知-行动-学习 + 任务基线对比。

---

## 关键设计点

### 1. 智能体不绑定 cohort 个体数

CognitiveAgent 锚定到 `(cellIndex, lineageId)`,但 `count` 独立追踪。这意味着 phase 1 不需要重写 cohort 生态 — agent 是个**新选择层**,与 P13 殖民地正交。未来 phase 可让 agent 控制其 cohort 成员行为(尚不做)。

### 2. 有限观测 + 离散化

`observe()` 只读自己 cell + 最多 3 个邻居(可控 fanout),不暴露全局状态。
`discretise()` 把 3 维(temperature × nutrient × neighbour-count)各分 3 档 → 27 状态,刚好够 Q-table 学又有泛化能力。

### 3. 能量 + 记忆成本

每步扣 `cognitionCostJ` = 1 J/agent(可在 `stepCognitiveRegistry` 改),从 `externalEnergyInJ` 走。Q-table 超过 256 entries 时 LRU 驱逐最旧 — 真实 agent 在资源约束下学。

### 4. 严格验收信号

`applyAction` 的 reward 只在落到**全局**最富 cell 时给 +50。这避免"温和" shaping(random 也能靠运气赢),让 Q-learning 必须真正学会路径规划。

### 5. 与 P12/P13 同模式

- 放 `ExperimentBook.cognition` 而非 `WorldState` → 不污染行星账
- v2 checksum 仍只覆盖 5 基础字段 → 旧存档 100% 兼容
- `Projection.cognition` 携带累计状态 → silent auto-step 也能 UI 显示
- `internalTick` silent 推进 → 主 tick 跑 P14 自动学习,不污染事件流

---

## 测试矩阵

| 测试 | 验证 |
|---|---|
| `discretise maps continuous observation into 27-state bucket` | 3×3×3 离散化正确 |
| `applyAction: move-best picks the richest neighbour and rewards only on the goal cell` | 局部观测 + 严格 reward shaping |
| **`Q-learning beats random on the foraging task`** | **P14 验收门** |
| `Q-learner fills its Q-table over the episode` | 学习确实发生 + memory bound 生效 |
| `cognitionLoad: attaches one agent per cohort` | 1 agent/cohort |
| `cognitionStep: debits externalEnergyInJ for cognition cost; silent mode does NOT emit a reply` | 能量成本 + silent 模式 |
| `cognitionStep non-silent: emits a cognition reply` | 非 silent 模式正常 |
| `cognition + chemistry + colonies coexist` | 三子系统独立 + 都在 projection 上 |

---

## 仍待用户拍板(战略层 D 4 项)

无新增,沿用。下一步候选:

- **F**:战略 D 4 项你拍板
- **G**:10 轮改动汇总提 PR(20 commit)
- **H**:**P14 phase 2**:接入 UI(主界面/新 dialog 显示 Q-table 大小/平均 reward/episode) + 真实多任务(觅食 + 避热 + 社交)+ 多智能体
- **D**:MIST 96 MB 数据接入(P10-C 闭环)

P14 phase 1 已具备基础接口 + 验收门。**要让它真在产品里"学到东西"被用户看见,UI 接入是 P14 phase 2 的高 ROI 项**。请选下一步。
