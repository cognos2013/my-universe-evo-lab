# V14-2 视觉路线交付报告(A + C)

> 日期:2026-09-07
> 阶段:V14 视觉路线 phase 2 — Three.js 真 3D 渲染 + 多快照持久化对比
> 工作区副本:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
> 原始目录:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(完全未动,无 V14 痕迹)

## 一、本轮决策(用户拍板)

| 选项 | 选择 | 依据 |
|---|---|---|
| A. V14 phase 2:Three.js / WebGL 升级 | **做** | 用户明确"你要找开源的免费能力,不要让我提供给你凭证" — Three.js 是开源免费的(MIT 许可),已用在 planet view |
| B. 真实 World Labs / Marble / Atlas / Spark API 接入 | **不做** | docs/04 要求 API key + 商业条款,用户无凭证;改走开源路径 |
| C. V14 资产持久化 + 多 v14 实验对比 | **做** | 横向比较同一 branch 不同时刻 / 不同 kind 的 3D 快照 |

## 二、本轮交付

### 2.1 A:Three.js 真 3D 渲染(替代 v14-1 Canvas 2D)

| 改动 | 位置 |
|---|---|
| `import * as THREE from 'three'` + `import { OrbitControls }` | src/app/v14.ts |
| `THREE.WebGLRenderer({ canvas, antialias: true, alpha: true, powerPreference: 'high-performance' })` | 3 个 canvas 各一个 |
| `THREE.BufferGeometry` 直接接 `V14Snapshot.vertices/colors/indices`(`BufferAttribute` 拷贝 typed array) | buildMeshFromResult |
| `MeshStandardMaterial({ vertexColors: true, roughness: 0.65, metalness: 0.05, flatShading: false })` | 同上 |
| `geometry.computeVertexNormals()` + `geometry.computeBoundingSphere()` | 同上 |
| 3 灯(ambient + key + fill)+ 软冷暖对比 | makeCanvasCtx |
| `OrbitControls`(鼠标拖动旋转 / 滚轮缩放 / damping 阻尼)+ 距离限位 | 同上 |
| 自动 `frameCamera(radius)`:相机距 = `max(0.5, radius * 3.0)`,留头部空间 | renderSnapshot |
| dispose 旧 mesh(geometry + material)避免 GPU 内存泄漏 | disposeMesh |
| resize listener(防抖到 rAF)+ 关闭 dialog 取消 frame loop | dialog close |
| 3 个独立 canvas(单 / 对比左 / 对比右),各自 renderer / scene / camera / controls | CanvasCtx × 3 |

**收益**:vs v14-1 Canvas 2D 投影 + z-sort:
- 真透视投影 + 光照(GPU)
- 鼠标拖动 / 滚轮缩放(无需手写)
- 20480+ 三角流畅(Canvas 2D 在 ~5000 三角就开始掉帧)
- WebGL 抗锯齿 + alpha
- 顶点法线计算 → 真实光照

### 2.2 C:V14 多快照持久化

#### 2.2.1 类型

| 改动 | 位置 |
|---|---|
| `V14Snapshot` 接口(完整 mesh + 元数据:id / label / createdAtTick / createdAtBranch / spec / backend / sourceLabel / vertices / colors / indices / boundingRadius / durationMs / createdAtMs) | src/simulation/visual/types.ts |
| `V14SnapshotSummary`(轻量摘要,不含 mesh)— 给 projection channel 用 | 同上 |
| `summariseV14Snapshot(s)` helper | 同上 |
| `defaultV14Label(spec, tick)` — "tick N · kind/style/res" 自动格式 | 同上 |
| `makeV14Snapshot({ id, spec, result, tick, branch, label?, createdAtMs? })` 工厂 | 同上 |

#### 2.2.2 ExperimentBook

| 改动 | 位置 |
|---|---|
| `v14Snapshots: V14Snapshot[]` 字段(替代旧 `v14Result` 单字段) | src/experiments/book.ts |
| `v14ActiveId: string \| null` 字段 | 同上 |
| `v14Spec / v14Backend / v14Result` 改为 getter,mirror 当前 active 快照(背书读路径) | 同上 |
| `activeV14()` — 查 active 快照 | 同上 |
| `pushSnapshot(snap, makeActive=true)` — 追加 + 可选激活 | 同上 |
| `selectSnapshot(id)` — 切换 active(throw if missing) | 同上 |
| `removeSnapshot(id)` — 删除 + 旋转 active 指针到列表末尾 | 同上 |
| `renameSnapshot(id, label)` — 原地改名 | 同上 |
| `v14Summaries()` — 投影摘要列表 | 同上 |
| `export()`:把 v14Snapshots 数组每个的 Float32Array / Uint32Array 转 plain `number[]` | 同上 |
| `import()`:把 plain array 复原 typed array + 兼容旧 v14-1 archive(单 `v14Spec/v14Backend/v14Result` 三元组 → 1-snapshot 列表,标 `(legacy v14-1 import)`)| 同上 |
| `known` set 加 `v14Snapshots / v14ActiveId` 字段 | 同上 |

#### 2.2.3 Controller

| 改动 | 位置 |
|---|---|
| `Projection.v14` 改:`{ snapshots: V14SnapshotSummary[]; activeId: string \| null }`(不再 nullable,空列表也合法) | src/workers/controller.ts |
| `RequestType` 加 `'v14Select' / 'v14Delete' / 'v14Rename'` | 同上 |
| `Reply` 加 `{ type: 'v14'; payload: { snapshot, activeId } }` + `{ type: 'v14List'; payload: { snapshots, activeId } }` | 同上 |
| `__InternalHandlerContext` 替换旧 `setV14`,加 4 个 mutator:`setV14Snapshot(snap, makeActive)` / `setV14Active(id)` / `removeV14Snapshot(id)` / `renameV14Snapshot(id, label)` | 同上 |
| `#ctx()` 实现:每个 mutator 写 book + 同步 `__v14` 缓存 + 触发 projection | 同上 |
| `handleV14Generate` 改造:`crypto.randomUUID()` 分配 id → `makeV14Snapshot` 工厂 → `ctx.setV14Snapshot(snap, makeActive)` → emit `{ type: 'v14', payload: { snapshot, activeId } }` | 同上 |
| `handleV14Select`:校验 id 存在 → setV14Active → emit v14List | 同上 |
| `handleV14Delete`:校验 id 存在 → removeV14Snapshot → emit v14List | 同上 |
| `handleV14Rename`:trim + 长度校验(>80 reject)+ renameV14Snapshot → emit v14List | 同上 |
| `handlers` 字典加 v14Select / v14Delete / v14Rename | 同上 |

#### 2.2.4 UI:3 列布局 + 列表 + 对比模式

| 改动 | 位置 |
|---|---|
| 3 列 grid:`300px 240px minmax(0,1fr)`(控制 / 列表 / 视图) | src/app/style.css `.v14-grid` |
| 中间栏 snapshot 列表:每行显示 label + tick + kind/style/res + 顶点计数 + 选中/改名/删除按钮 | index.html + v14.ts renderList |
| 右侧栏:单模式 1 个 canvas / 对比模式 2 个 canvas + 2 个 select 选左右快照 | index.html + v14.ts setCompareMode |
| `对比两个快照` 切换按钮:切到对比模式时自动选最新 2 个快照,左右 select 切换 | v14.ts |
| 选中快照:通过 v14Select handler 切 active,然后从 book fetch 完整 mesh 重新渲染(Web Worker 那边再 export 一次 main thread 解析) | renderSnapshot + fetchSnapshot |
| 删除快照:confirm 弹窗 + v14Delete handler + 同时清理当前显示该快照的所有 canvas | doDelete |
| 改名:prompt 弹窗 + v14Rename handler | doRename |
| 视图元数据卡片(vertices / triangles / bbox radius / source / current label) | renderMeta |
| Three.js 渲染 + OrbitControls + 3 灯 + frame loop + resize debounce | makeCanvasCtx + frameLoop + scheduleResize |

### 2.3 测试(`tests/v14.test.ts`,整体重写)

| 测试 | 覆盖 |
|---|---|
| 7 个 validateV14Spec 边界 | 5 kind accept / unknown kind / resolution out of range / unknown style / ALL_PROCEDURAL_KINDS=5 |
| 4 个 inRepoBackend 形状 | terrain vertex/index 数量 + 决定性 + 不同 seed 区别 + resolveBackend 默认 |
| 1 个 backend honest error | worldLabs / atlas / spark 3 个统一抛 API key + commercial |
| 1 个 generateWorld router 路由 | 默认 inRepo / 显式 backend 报错 |
| **13 个 controller + book 集成** | |
| └ v14Generate push snapshot + make active | 列表长度=1 / active 指向新 snap / reply payload.snapshot |
| └ v14Generate × 2 | 列表长度=2 / active 旋转到新 |
| └ keepActive: false | 列表长度=2 / active 不变 |
| └ v14Select | 切 active + emit v14List |
| └ v14Select 错误 | unknown id reject |
| └ v14Delete | 删 + active 旋转到剩下那个 |
| └ v14Delete 最后一个 | 列表=0 / active=null / v14Spec/v14Result getter 返 null |
| └ v14Delete 错误 | unknown id reject |
| └ v14Rename | 原地改 label |
| └ v14Rename 错误 | empty / >80 字符 reject |
| └ v14Generate 4 个边界 | unknown kind / resolution / backend / worldLabs |
| └ 5 kind 全跑 | 每个 kind produce non-empty snapshot |
| └ book round-trip 2 snapshots | export / import 后列表 2 个 + active 保持 + 顶点数据 byte 对应 |
| └ book 兼容 v14-1 archive | 旧 JSON 缺 v14Snapshots 字段 → 自动迁移为 1-snapshot 列表 + 标 legacy |

**累计 219/219 tests + build + dev server 全绿**

## 三、修的 1 个 bug

| 现象 | 根因 | 修复 |
|---|---|---|
| `handleV14Rename` 对 200 字符 label 不 reject | handler 静默 `slice(0, 80)`,200 字符变 80 字符通过 | 改成显式校验:trim 后 length > 80 抛 'label 不能超过 80 字符' |

## 四、验证

```bash
cd /Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/
npx tsc --noEmit              # 0 errors
npm run check                 # 219/219 pass (V14 + V14-2 全跑)
npm run build                 # 154ms, dist 620 KB JS (含 three.js tree-shaken)
npm run dev -- --port 9151    # HTTP 200, V14 dialog 3 列布局
```

## 五、ROI 排序(下一步选项)

| 选项 | 价值 | 成本 | 推荐度 |
|---|---|---|---|
| E. 19 轮全部改动汇总提 PR | 一份干净 commit 树(60+ commit) | 2 小时 | **高** |
| F. V14 快照跨 branch 对比 | 同一主时间线不同分支下的快照一起比较(fork + V14 → 可视化"如果当年不是这种生命...") | 1 天 | 中 |
| G. V14 自动 batch 生成(参数扫描) | 用 P17 batch 跑 V14 多 seed / 多 kind,把"哪些 seed 看起来最像树"做成直方图 | 半天 | 中 |
| H. 真实 in-browser 3D 资产格式(.glb / .gltf)导出 | 让 V14 资产可在 Blender / web 3D viewer 重用 | 半天 | 低(没用户需求) |
| I. WebXR(VR/AR)模式 | Three.js 本身就支持,加 `WebXRButton` 即可 | 1 天 | 低(用户群窄) |

## 六、留什么没做(诚实交代)

- **真实 World Labs / Marble / Atlas / Spark 接入**:用户明确"找开源能力,不提供凭证",走 Three.js in-house 路线;3 个 honest stub 还在 `worldLabs.ts`,未来用户改主意随时能接
- **shader 高级效果**:目前 `MeshStandardMaterial` 默认参数,没有金属 / 透明 / 法线贴图扩展
- **shadow map**:3 灯无阴影投射(性能优先)
- **web worker 渲染**:Three.js 渲染在主线程;大数据集(>100k 三角)可以 offload 到 OffscreenCanvas + Worker(phase 3)
- **后端持久化跨设备**:snapshot 只存在客户端 book.json,要跨设备分享得导出 .json 再导入
- **资源管理**:200 个 snapshot 之后 UI 列表会卡(>1000 行 DOM),需要虚拟滚动
- **V14 ↔ P14 耦合**:V14 仍是独立子系统,没有接 cognition state(humanoid 表情随 reward 变)— 那是 D 选项
- **WASM 化 / 跨浏览器一致性 / 仓库内 auto-continue**:仍是战略层 D 未决,等用户拍板

## 七、本轮改动汇总(commit-ready)

```
修改文件:
  src/simulation/visual/types.ts        +V14Snapshot +V14SnapshotSummary +summariseV14Snapshot +defaultV14Label +makeV14Snapshot
  src/experiments/book.ts               +v14Snapshots/v14ActiveId 字段 +5 个 CRUD helper + getter 背书 +export/import 适配 +legacy v14-1 兼容
  src/workers/controller.ts             +Projection.v14 改 snapshots+activeId +v14Select/Delete/Rename handler +4 个 mutator +handlers 字典
  tests/handlers.test.ts                +setV14Snapshot/setV14Active/removeV14Snapshot/renameV14Snapshot mock
  tests/v14.test.ts                     整体重写 23 tests(7 验证 + 4 inRepo + 13 controller/book 集成)
  src/app/v14.ts                        整体重写 23 KB,Three.js 渲染 + snapshot 列表 + 对比模式
  src/app/style.css                     .v14-grid 3 列 + .v14-snap 列表 + .v14-compare-canvases + responsive
  index.html                            v14-dialog 3 列布局 + v14-list-pane + v14-compare-pane + 2 个 select
  src/app/main.ts                       mountV14 注释更新
```

dev server http://localhost:9151/(持续运行)— 打开 → 3D 视觉实验 → 看新 3 列布局(控制 / 列表 / 视图);生成几个不同 kind + style 的快照,然后"对比两个快照"。

## 八、用户未决选择(继承)

1. **战略层 D 4 项**:WASM 化触发 / 跨浏览器一致性 / 仓库内 auto-continue(14 视觉路线已实质完成 A+C,可视为部分解决)
2. **下一步推进方向**:本轮后建议 E(19 轮汇总提 PR);若用户想要更多 V14 工作,做 F(跨 branch 对比)或 G(batch 扫描)
3. **真实 API 接入**:用户明确"开源免费路径",暂无 B 需求;如要 B,得用户自找免费替代(如 Meshy / LumaGenie 免费层 / Tripo3D 等)— 这些需要本地 API key 配置

—
Mavis 2026-09-07
