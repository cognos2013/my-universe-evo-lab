# 夜间进度与验收

开始：2026-09-05 23:24（Asia/Shanghai）。用户授权按阶段自动实现、测试审核、记录后推进。

## 执行规则

每阶段对照详细设计验收，运行自动测试并检查实现是否真正覆盖要求。失败先修复，不降标准报通过。成果和关注事项记录到本文及阶段报告。最初夜间范围为首版 P1—P6；当前按最新授权自动接续 P10—P17，具体边界以主线计划为准，不声称已实现科研级宇宙或完整文明模型。

首版 P0—P6 已完成阶段验收。按用户最新授权，自动化 `automation` 已更新为“我的宇宙 P10—P17 自动推进”并启用，每 30 分钟接续检查；实际实现继续进行。完成阶段后进入下一阶段，全部通过后暂停；宿主离线时无法接续。后面的早期“已暂停”日志保留作历史记录。

## 状态

| 阶段 | 状态 | 验收证据 | 关注事项 |
| --- | --- | --- | --- |
| P0 合同与种子 | 已通过 | 28 测试、类型检查、3 场景导入导出 | 旧格式是合同原型，新增运行状态须升级 |
| P1 环境与资源账 | 已通过 | 33 测试；100 tick 批量运行；reports/P1-tests.txt | 简化热平衡，无真实天气；旧 P0 存档明确拒绝 |
| P2 生命演化 | 已通过 | 42 测试；100 tick 生态推进；reports/P2-tests.txt | 预定义性状空间、同批突变共享性状；预算超限暂停 |
| P3 可视交互 | 已通过 | 44 核心测试、2 浏览器用例、生产构建 | WebGL 本地兼容路径；Three.js 包较大 |
| P4 存档重放 | 已通过 | 46 核心测试、4 浏览器用例 | 本地 IndexedDB；检查点重放而非倒算 |
| P5 干预分支 | 已通过 | 52 核心测试、5 浏览器用例、生产构建 | 单次结果不代表概率；20 分支预算 |
| P6 综合验收 | 已通过（生态 MVP 范围） | 56 核心测试、8 浏览器用例、场景校验、构建与性能通过；reports/final-audit.json | 全浏览器/GPU 内存和跨浏览器重放未验证；详见 P6 报告 |

| P7 长期历史管理 | 已通过 | 58 核心测试、8 浏览器用例、构建与性能回归；reports/final-audit.json | 检查点稀疏后重放更慢；容量为软目标，曲线抽样可能省略尖峰 |

| P8 观察精度与宇宙总览 | 已通过 | 61 核心测试、10 浏览器用例、构建与性能回归 | 宇宙背景与生态时钟独立；超精细档性能不等同标准档 |

| P9 旧世界细分与同步时间 | 已通过 | 65 核心测试、13 浏览器测试、全量审核 | 同步为同一生态日，不跳算亿年；细分增加负载 |

| P10-A 气体与恒星群 | 子阶段已通过 | 69 核心测试、14 浏览器用例、全量审核 | 预置晕和教学参数；P10-B/C 未完成 |

| P10-B 引力聚集与装配 | 限定规模验收通过 | 76 核心测试、14 浏览器测试、含 500 Myr 引力动态审核 | 孤立软化点晕、简化受束缚合并；P10-C 未完成 |

## 日志

- 23:24：从 P1 接续。每阶段测试日志与报告保存至 `reports/`，不以测试数量替代验收覆盖。

- P1 验收通过：实现三档闭合球面网格、按实际面积计热交换、当地物质播种、能量账、原子环境步与命令行。发现并修复热交换被覆盖的问题，增加非均温回归。320 单元本机约 2408 tick/s（环境单独运行，非完整模拟性能）。

- P2 验收通过：遗传、资源竞争、死亡回收、迁移和反照率反馈已接通。冷暖环境在多种子实验中产生不同优势；关闭突变不产生新性状；输入顺序变化与序列化不改变后续结果。100 tick 本机约 243 tick/s（320 单元）。

- P3 验收通过：桌面/390px 手机浏览器实际运行，无脚本错误；单步、运行、图层、区域观察、新世界可操作。镜头/检查面板不改变模拟状态；截图保存于 reports/P3-*.png。

- P4 验收通过：历史恢复、刷新持久化、导入导出与存储异常浏览器测试通过。回溯保存不会丢失原来已计算的未来；记录完整初始检查点与随机状态。

- P5 验收通过：五种干预、自动创建子分支、分支切换、同一时刻对照和完整实验文件恢复可用。UI 对照截图 reports/P5-comparison.png。

- P6 验收通过：2026-09-06 07:43（Asia/Shanghai）重新运行全部审核，五项任务退出码均为 0。补齐知识导览、模型边界、长任务取消、资源精度一致性与存档边界保护。阶段报告、晨间报告及最新测试证据已归档；夜间接续已暂停。

- P7 验收通过：按用户“继续开始下一步”接续长期实验管理。检查点数量与体积自动整理，曲线样本有界，界面显示历史容量及抽样状态；完整历史重放及干预不变。详见 [P7 报告](11-P7-长期历史管理.md)。夜间自动化仍保持暂停。

- P8：根据用户反馈增加 20,480 区域、默认 5,120 区域和近距离观察；新增可调年代的宇宙背景总览。已有存档不变。全部审核通过，详见 [P8 报告](12-P8-观察精度与宇宙总览.md)。

- P8 修复：收到“Cohort budget exceeded”反馈。将报错改为包含实际需求与上限的中文，增加队列用量及“扩充计算容量”。从完整状态创建扩容分支，容量翻倍（最低 20,000，最高 100,000），更新规则摘要，保留父分支与随机状态。阻塞后可继续、完整实验恢复与上限拒绝均通过；最终 63 核心测试、11 浏览器测试及全量审核通过。扩容增加内存需求，不保证无限演化；规则摘要不同的分支目前不支持直接 A/B 对照。

- P9：旧存档可保守细分为新分支，保留当前日期、生命与收支；宇宙默认共享生态日调度，快速背景浏览需取消同步。全部验收通过。详细成果与限制见 [P9 报告](13-P9-旧世界细分与同步时间.md)。

- 2026-09-06 文档增补：按用户要求参考 World Labs 官方 Marble、Atlas、World API 与 Spark 资料，新增 [世界模型与逼真场景开发方案](14-世界模型与逼真场景开发方案.md)，并更新项目方案和详细设计。方案区分模拟真值、空间语义和外观生成，列出 V1—V5 后续工作包及验收。本次仅文档修改，未接入外部生成服务、未新增运行依赖、未声称视觉原型已完成。文档链接与结构检查通过，未重复运行无关的代码测试。

- 主线调整：按用户要求，暂缓 V 视觉阶段，优先模拟能力。新增 [P10—P17 主线](15-模拟能力主线-P10至P17.md)。P10-A 气体冷却、恒星群形成/死亡/回流、可检查面板与完整存档已通过验收；P10 整体保持未完成，下一步 P10-B 引力聚集与装配。详见 [P10-A 报告](16-P10A-气体与恒星群验收.md)。

- 按用户要求将开发服务端口调整为 9150，同步测试配置和运行文档。浏览器存储按来源隔离，原 5173 存档需通过导出/导入转移到 9150。

- P10-B 完成局部引力与装配验收，自动进入 P10-C 的资料/校准准备。开启 P10—P17 自动接续，后续不用用户逐阶段重复授权；不降低验收标准。详见 [P10-B 报告](17-P10B-引力聚集与装配验收.md)。P10-C 下一步核实恒星质量分布、演化轨道和元素产额的可用原始资料，冻结参数和数据版本后实现，尚未报通过。

- P10-C 接续：完成带来源和有效质量区间的 IMF 解析积分基础，独立数值积分与分箱细化测试通过；79 核心测试及类型检查通过。尚未接入运行中的恒星群；寿命、元素与反馈待实现，阶段保持未通过。后续数据选择、金属丰度适用边界和迁移步骤见 [P10-C 开发记录](18-P10C-恒星演化开发记录.md)。自动化继续，无需用户操作。
