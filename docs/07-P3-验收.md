# P3 可视交互验收

状态：通过。证据：[核心日志](../reports/P3-tests.txt)、[桌面截图](../reports/P3-desktop.png)、[手机截图](../reports/P3-mobile.png)。

- 44 项核心测试、2 个 Playwright 浏览器用例通过，生产构建成功。
- 真实 Worker 计算驱动三维网格颜色与指标，没有用定时假数值代替模拟。
- 暂停、单步、按天推进、新建场景、5 种图层、点击区域与键盘可访问的区域入口可用。
- 地表/温度/生物量/营养/优势谱系图层有图例；图例说明和当前模型单位可见。
- 观察与投影经状态哈希验证不消耗随机数、不改变世界；暂停取消后续推进。
- 本地 Chrome 无运行错误，390px 页面无横向溢出，视觉截图已人工核对。

关注事项：首版使用 Three.js WebGL 兼容路径，WebGPU 尚未启用；主要脚本约 557 kB 未 gzip，构建有分包建议但未阻塞。图表当前是观察采样，P4 接入正式时间历史。图层颜色是数据映射，不是卫星影像。P4 尚未通过前，刷新页面不会保存世界。

本地启动：`npm run dev`，地址 `http://127.0.0.1:9150/`。当前没有外网发布。
