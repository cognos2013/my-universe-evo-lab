# V14-4 视觉路线交付报告(I)

> 日期:2026-09-07
> 阶段:V14 视觉路线 phase 4 — V14 ↔ P15 文明聚落耦合(每聚落生成专属 3D 天际线)
> 工作区副本:`/Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/`
> 原始目录:`/Users/fuj2ee/Documents/ChatGPT/我的宇宙/`(完全未动,11:37 mtime)

## 一、本轮决策(用户拍板)

| 选项 | 选择 | 依据 |
|---|---|---|
| I. V14 ↔ P15 文明聚落耦合(每聚落生成专属 3D 天际线) | **做** | docs/15 I:V14 真正"为生态/文明服务",让用户用 3D 直观看到"如果当年是这种制度..."的可视化 |
| A. Three.js 升级 | 已做(V14-2) | — |
| B. 真实 API 接入 | 砍 | 用户明确"开源免费" |
| F. 跨 branch 对比 | 已做(V14-3) | — |
| G. batch 参数扫描 | 已做(V14-3) | — |

## 二、本轮交付

### 2.1 类型:`V14WorldSpec` 加 `city` 字段 + ProceduralKind 加 `'cityscape'`

| 改动 | 位置 |
|---|---|
| `ProceduralKind` 加 `'cityscape'`(第 6 个 kind) | src/simulation/visual/types.ts |
| `ALL_PROCEDURAL_KINDS` 数组加 `'cityscape'` | 同上 |
| `V14WorldSpec` 加 optional `city` 字段:settlementId / label / population / knowledgeLevel / institution / taxRate / techCount / food / cellIndex / cellAreaM2 / cellNutrientMu / cellTemperatureK | 同上 |
| `validateV14Spec` 接受 `'cityscape'` kind | 同上 |
| `validateV14Spec` 校验 city 字段:settlementId 非空 / population ≥ 0 / knowledgeLevel ≥ 0 / institution ∈ {public, private, mixed} / taxRate ∈ [0,1] / techCount ≥ 0 / food ≥ 0 / cellIndex 整数 ≥ 0 | 同上 |

### 2.2 inRepo:第 6 个 generator — `generateCityscape(spec, seed)`

接收 `spec.city` 字段(由 controller 解析 P15 settlement 填好),产 procedurally generated cityscape mesh,**所有几何 / 颜色参数都直接 mirror settlement 状态**:

| Settlement 属性 | 影响 mesh |
|---|---|
| `population` | 建筑数 = `clamp(2, 50, round(population / 5))` — 50 人 10 栋,5000 人 50 栋(cap) |
| `knowledgeLevel` | 建筑基础高度 = `0.8 + knowledge * 0.4 + tech * 0.15 + (public ? 0.4 : 0)` — 知识 + 0.4 倍/级 |
| `techCount` | 同上 + 建筑细节 |
| `institution` | 屋顶形状:public = 4 面金字塔(dome-like);private = 8 边锥(spire);mixed = 平板 |
| `cellAreaM2` | 城市边长 = `min(8, sqrt(area) * 0.6)` — 城市占 cell 60% |
| `cellNutrientMu` | 墙体色饱和度 — 富营养 = 暖砂色,贫瘠 = 冷灰 |
| `food` | 屋顶金度 — 食物充足 = 金色屋顶,稀缺 = 灰顶 |
| `cellIndex` | 0 = 城市中心(标志建筑,最高最显眼) |

加 1 个 ground plate quad(在 y=-0.01)避免 z-fight。`sourceLabel` 包含 `"{N} buildings, {inst} inst, knowledge {K}, food {F}"` 让 UI banner 直接显示城市参数。

### 2.3 Controller:`handleV14Generate` 解析 settlement → spec.city

| 改动 | 位置 |
|---|---|
| 当 `spec.kind === 'cityscape'`:从 `ctx.book.settlement` registry 找匹配的 settlement(优先用 spec.city.settlementId 指定,fallback 到第一个) | src/workers/controller.ts |
| 把 settlement 全状态(人口/知识/制度/税率/技术数/食物/cell 面积/cell 营养/cell 温度)+ cell 营养写回 `spec.city` | 同上 |
| 若 `spec.city.settlementId` 显式指定但找不到 → 抛 `'P15 settlement 不存在：{id}'`(loud fail,UI 端能 catch) | 同上 |
| Snapshot label 自动取 `cityscape · {settlement.label}` | 同上 |

### 2.4 UI:P15 settlement 列表加"📷 生成 3D 天际线"按钮

| 改动 | 位置 |
|---|---|
| `mountSettlement(send, onStatus, onGenerateCityscape?)` 加第 3 个可选 callback 参数 | src/app/settlement.ts |
| `bySettlement` 每行(未解散的 settlement)加 "📷 生成 3D 天际线" 按钮 → 调 `onGenerateCityscape(settlementId, label)` | 同上 |
| 按钮放在 top 行下面、单独 div,避免和数字混淆 | 同上 |
| 旧生成 template 流程不变 | 同上 |

### 2.5 `mountV14` 返回 `{ render, prefillCityscape }`

| 改动 | 位置 |
|---|---|
| mountV14 签名改为返回 `V14Panel { render: (proj) => void; prefillCityscape(id, label) => void }` | src/app/v14.ts |
| `prefillCityscape(id, label)`:把 kind 选 cityscape + label 输入框填 `cityscape · {label}` + 设置 status + 自动 click generate 按钮 → 触发 controller 解析 + 生成 | 同上 |
| main.ts:`const v14Panel = mountV14(...)`;`const renderV14 = v14Panel.render`;`mountSettlement(..., (id, label) => { v14Panel.prefillCityscape(id, label); v14Dialog.showModal(); })` | src/app/main.ts |

### 2.6 测试(`tests/v14.test.ts`,加 6 个新)

| 测试 | 覆盖 |
|---|---|
| `cityscape generator with no city context still produces a mesh` | spec.city 为 null 时,fell back 到默认 population=30 → 6 栋建筑,有 mesh,顶点 ≥ 100 |
| `cityscape generator scales building count with population` | 人口 30 vs 1000 → 大城市顶点 > 小城市顶点 |
| `cityscape source label includes settlement state for the UI banner` | sourceLabel 包含 "public" / "knowledge" / 人口数,UI 可直接显示 |
| `handleV14Generate with kind: cityscape falls back to a small generic city when no P15 settlement loaded` | 空 settlement registry → spec.city 仍为 null / generator 仍跑通 |
| `handleV14Generate with kind: cityscape resolves a real P15 settlement into the spec` | load Aurelian 聚落(80 人 / 知识 4 / public / 2 tech)→ spec.city 字段全部正确填(snapshot label 含 "Aurelian")|
| `handleV14Generate with kind: cityscape and a specific settlementId that does not exist throws` | 显式 settlementId='no-such' → 抛 `settlement 不存在\|no-such-settlement`(loud fail)|
| `ALL_PROCEDURAL_KINDS has 6 entries (cityscape added in V14-4)` | 验证数组长度 + 包含 cityscape |

**累计 230/230 tests + build + dev server 全绿**

## 三、修的 1 个小问题

| 现象 | 根因 | 修复 |
|---|---|---|
| 测试 `ALL_PROCEDURAL_KINDS has 5 entries` 失败 | ProceduralKind 从 5 增到 6(`cityscape`) | 改测试为 `has 6 entries` + assert `includes('cityscape')` |

## 四、验证

```bash
cd /Users/fuj2ee/workspace/跨智能体管理项目/我的宇宙项目学习/我的宇宙/
npx tsc --noEmit              # 0 errors
npm run check                 # 230/230 pass (V14-1+2+3+4 全跑)
npm run build                 # dist 140 KB worker / 626 KB JS / 25 KB CSS
npm run dev -- --port 9151    # HTTP 200
```

## 五、ROI 排序(下一步选项)

| 选项 | 价值 | 成本 | 推荐度 |
|---|---|---|---|
| E. 21 轮全部改动汇总提 PR(80+ commit) | 一份干净 commit 树,review 友好 | 2 小时 | **高** |
| H. V14 资产导出 .glb / .gltf | Three.js `GLTFExporter`,把 cityscape / terrain 导出给 Blender 二次编辑 | 2 小时 | 中 |
| J. cityscape 演化(每个 step 自动生成当前状态快照) | 真正"看城市随时间成长" | 半天 | 中(用户没明示) |
| K. V14 ↔ P14 cognitive(cognition 状态影响 humanoid 表情) | 把 humanoid 当作 agent 3D 可视化 | 半天 | 中 |
| L. 把所有 6 个 kind 的 generator 提到 50 fps headless benchmark | 数据集:每个 kind × 每个 resolution × 每个 style 的实测 ms + 顶点数 | 2 小时 | 低(纯数据,无用户消费场景)|

## 六、留什么没做(诚实交代)

- **cityscape 不动 settlement state**:V14 读完 settlement 状态生成 mesh,但 mesh 不会反向写回 settlement(单向耦合)。如要"用 3D 编辑改 cell 营养",需要双向同步 + 用户授权
- **cityscape 没有"昼夜循环"动画**:目前静态 mesh。Three.js 可加 directional light rotation 模拟日照,但 phase 5 再说
- **没有"对比两个聚落"功能**:可以加 P15 列表多选,生成多个 cityscape 一次 + V14 对比模式横向并列(2 行按钮 → 选 2 个 settlement)
- **没有"批量 cityscape"**:batch 参数扫描只覆盖 5 个 inRepo kind,cityscape 需要新路径(spec.city 需 per-run 替换)— 短期可不做
- **WebXR 模式 / 真实 API 接入 / WASM 化 / 跨浏览器一致性 / 仓库内 auto-continue**:战略层 D 未决,等用户拍板

## 七、本轮改动汇总(commit-ready)

```
修改文件:
  src/simulation/visual/types.ts        +ProceduralKind 'cityscape' +V14WorldSpec.city 字段 +validate 校验
  src/simulation/visual/inRepo.ts       +generateCityscape(spec, seed) +case 'cityscape' 路由
  src/workers/controller.ts             +handleV14Generate 解析 P15 settlement + label 自动 'cityscape · {name}'
  src/app/v14.ts                        mountV14 改返回 { render, prefillCityscape }
  src/app/settlement.ts                 +onGenerateCityscape callback +每行按钮
  src/app/main.ts                       v14Panel + mountSettlement 注入 prefillCityscape
  tests/v14.test.ts                     +6 tests(cityscape + 反射 + 边界)
```

dev server http://localhost:9151/ — 操作路径:
1. 打开"聚落与文明"→ 选 templates → 生成聚落 → 推进
2. 在 by-settlement 列表里任一未解散的 settlement 旁点"📷 生成 3D 天际线"
3. 自动跳到 3D 视觉实验 dialog,生成 cityscape mesh
4. 看到城市天际线:建筑数 = pop/5,高度 ∝ knowledge,屋顶形状 = institution(public=金字塔/private=尖顶/mixed=平顶),颜色 ∝ food/nutrient

## 八、用户未决选择(继承)

1. **战略层 D 4 项**:WASM 化 / 跨浏览器一致性 / 仓库内 auto-continue(14 视觉路线已实质完成 F + G + I,可视为进一步解决)
2. **下一步推进方向**:本轮后强烈建议 E(21 轮汇总提 PR),次推 H(导出 .glb)
3. **真实 API 接入**:用户明确"开源免费",暂无 B 需求

—
Mavis 2026-09-07
